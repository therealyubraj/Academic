A library management terminal based software written in Python.

Program wont correctly run on IDLE because of ANSI rendering issues.

If you can use terminal to use python, then:
```
python main.py
```
